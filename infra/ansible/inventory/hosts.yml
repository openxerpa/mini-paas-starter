all:
  children:
    production:
      hosts:
        server-1:
          ansible_host: 100.x.x.x  # Tailscale IP
    staging:
      hosts:
        server-2:
          ansible_host: 100.x.x.x
  vars:
    ansible_user: deploy
    ansible_python_interpreter: /usr/bin/python3
