---
- name: Bootstrap server with Docker and base directories
  hosts: all
  become: true
  roles:
    - docker
  post_tasks:
    - name: Create deploy base directory
      ansible.builtin.file:
        path: "{{ deploy_base_dir }}"
        state: directory
        mode: "0755"
