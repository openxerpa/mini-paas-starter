[defaults]
# Always pass -i explicitly: -i inventory/production or -i inventory/staging
# Defaulting to staging reduces risk of accidentally targeting production
inventory = inventory/staging
host_key_checking = False
pipelining = True
